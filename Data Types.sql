-- CREATE TABLE orders (
-- order_id INT AUTO_INCREMENT PRIMARY KEY,
-- order_date DATETIME,
-- order_total DECIMAL(10,2)
-- );

-- INSERT INTO orders (order_date, order_total)
-- VALUES (CURTIME(), 4);

SELECT *
FROM orders;

